1. All instances of 'title' must be changed to 'filename'
2. All instances of 'directory' must be changed to 'dir'
4. Remove 'New' from function titles when done (.js)
4b. Remove all non-camelcase functions when done (.py)
5. Remove 'Log' buttons from the beginning of functions when done
7. Add check that filename and dir are not empty on save
8. Work on the 'loaded' functionality in journals, customUI, CustomInput
  a. Needs to trigger overwrite or save new correctly
  b. (customInfo) Needs to remove clockIn if clocking out
  c. (customInfo) Needs to set overwrite only when imported from records
9. Add a rookie mode toggle in main that passes a boolean that useEffect can use to hide certain classes.
